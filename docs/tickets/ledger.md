# Migration ledger

Retired / amended / added inventory rows, one line per entry (shared format,
chunk-1 header): `<feature-row-id> | retired|amended|added | <ticket> | <reason>`.
Maintainer sign-off = approving the PR that edits the ledger line.

features/03 census +#d9-ui-switch | added | T13 | dual-UI switch control (T38) renders on BOTH page generations; the census diff shows it CHANGED between sides by design (label "Switch to new UI" vs "Switch to legacy UI", href legacy<->new with ui=legacy pinned toward legacy)
features/03 F4/F5/F6/F166 | amended | T13 | exact-mode fetch+gunzip+parse now runs inside the T16 worker load pipeline on the migrated page (parse off the main thread); same stream/buffered selection, loading-phase labels (F9), byte-identical parse and error surface - mechanism anchor moves from the inline bootstrap to lib/trace load.ts
features/01 census: #d9-ui-switch | added | T14 | Dual-UI switch control (T38). Registering the browser page in NEW_UI_ENTRIES makes the always-visible control render on BOTH sides, with per-side label/href ("Switch to new UI" -> /new/index.html vs "Switch to legacy UI" -> /index.html?ui=legacy). The old-vs-new affordance-census diff therefore carries exactly one CHANGED entry for #d9-ui-switch; everything else is zero-diff.
features/03 F147/F153 | amended | T19 | zoom->URL sync on the migrated page is debounced (150ms, one replaceState per zoom burst) and additionally carries the versioned view-state hash (docs/ui-inventory/05-url-view-state.md); legacy worker-zoom/offworker-zoom query semantics unchanged (F148/F149/F151/F152 intact - differ J5/J9 zero-diff, restore-on-load writes nothing)
features/03 F180 | amended | #740 | migrated api-mode flamegraph preserves inspection in `inspect`/`inspect_full`; canvas zoom remains URL-silent
features/03 census +.d9-copy-link | added | T19 | copy-link button in the migrated page header (both modes): flushes the pending debounced view-state write, copies location.href; minimal share affordance until chunk-2's status bar chrome
features/01 G8 | amended | T15 | dead column sort -> working sortable raw-results table on the migrated page (ADR-0004 section 1). All columns sortable, numeric for Trace Start/Seg #/Size, lexical otherwise; second click flips direction; sort rebuild preserves the checkbox selection; legacy page keeps the recorded dead affordance (walker asserts per-side).
features/01 C6 | amended | T15 | bucket-picker filter -> config-driven predicate on the migrated page (Finding 2 structural fix): page-URL ?bucket_filter= override (empty = no filtering, carried through URL syncs) > /api/config bucket_filter (server default "dial9"; AppState::with_bucket_filter) > "dial9" fallback. #607's Show-all toggle preserved on top; legacy page keeps the hardcoded predicate.
features/01 I2 (+F4/I3 display) | amended | T15 | parseKey mislabel -> unknown-layout keys render RAW on the migrated page via lib/trace/keys.ts's layout discriminant (Finding 1): raw table shows the full key across Service/Host/Boot; heatmap groups/labels by raw directory path (F4); titles/aggregation scope params carry no shifted svc/host (I3/H3/H6). Filename epoch/segIndex stay parsed (layout-independent). T14's legacy-keys.ts compat seam retired; legacy page keeps the shift.
features/01 F10 | amended | T15 | time-only heatmap axis -> date-carrying ticks (YYYY-MM-DD HH:MM:SS) on the migrated page when the visible span crosses a calendar-day boundary in the active TZ mode (Finding 3); tick count and the H1 selection-count format unchanged; legacy page stays time-only.
features/03 F38 | amended | T48 | #593 fix (maintainer ruling: option 1): search percentage now reports the highlighted-area share (union of INCLUSIVE counts of topmost matched frames / total in view) instead of matched SELF samples, and the matchedSelf>0 hiding guard is removed (mid-stack-only matches no longer suppress the figure); matches the F40 highlight, the export SVG's Matched: figure (F84), and flamegraph.pl. CORE-side change (flamegraph.js, sanctioned by T48), so BOTH page generations change together: legacy-vs-new differ stays zero-diff; old-vs-new carries the searched-checkpoint fg.searchStats delta as the single expected behavioral change. Regression: tests/core/flamegraph_search.test.ts (failing-first, demo-trace anchors).
features/03 F186 | added | T20 | unified `?` help overlay on the migrated flamegraph page, both modes (K3: ? was verified dead on legacy); F94 reference table is the content baseline; widget's own [info] overlay untouched
features/03 F187 | added | T20 | `f` fit: reset zoom to full tree preserving the search query (K4; the Escape cascade clears search first, F44 forbids that for fit); composed from released widget behaviors, frozen core untouched
features/03 F188 | added | T20 | `z` zoom undo over a bounded page-owned view history (K4); deliberately NOT URL state (05-url-view-state.md); name-path fidelity equals the F150 URL restore
features/03 F157 | amended | T20 | migrated page: an OPEN unified `?` overlay closes ahead of cascade stage 1; cascade byte-identical when it is closed
features/01 A8 | added | T20 | unified `?` help overlay on the migrated browser page (K3; NOTE: no legacy browser help surface exists at HEAD - the ticket's "exists" and K3's browser claim did not re-verify)
features/01 A9 | added | T20 | `/` focuses the active tab's search input (K3 one-vocabulary; focus-tolerant router, K5)
features/01 F21 | added | T20 | keyboard window selection on the heatmap (K2 fix): focusable plot (axe-serious scroll region fixed), Shift -> arrows -> Enter/Shift confirm through the same finalizeSelection as F12, Escape cancels, announced
features/01 D9 | amended | T20 | migrated page: Enter in any controls-bar field runs the search under the same enabled gate (K2 "no key submits search")
features/01 F12 | amended | T20 | cross-reference only: the new keyboard path (F21) lands on the same finalizeSelection; mouse behavior unchanged
features/02 F1 | amended | T25 | viewer-axis date qualification (no 04 finding id; S2/#137 time-legibility family): on the migrated viewer the time-axis track carries a MM-DD date prefix (MM-DD HH:MM:SS) on tick labels when the visible span's start and end fall on different calendar days in the active tz mode (absolute mode with resolvable clock-sync anchors); same-day spans and relative mode stay time-only. Same rule as T15's heatmap-axis amendment (features/01 F10), narrower prefix (MM-DD vs YYYY-MM-DD). Legacy viewer stays time-only. Also records the layout-seam adaptation: the axis draws at draw-area-relative x (canvas is drawW-wide, LABEL_W provided by the shared DOM gutter) instead of the legacy full-width canvas + inline LABEL_W offset; alignment asserted pixel-exact at three widths (src/pages/viewer/axis.test.ts). Adds uiPrefs.timeMode/tz to the store (T21 declared the seam, T25 landed the two fields, defaulted to legacy rel/utc; T33 drives them via the E1/E2 toggle buttons).
features/02 G20 | retired | T22 | legacy `selectedEvent` poll indicator was already DEAD (cleared on every lane click, no lane-render code, viewer.html:1299/5362 - leftover from an older design). The migrated lanes track (components/canvas/lanes) does not port it; the live in-lane pinned-custom-event highlight is the separate `selection.pinnedEvent.poll` mark (I5/K4, dispatched by T27). Row listed as retired in the G-row walk, not gated.
features/02 G19 | amended | T22 | worker-lanes legend relocated from the legacy toolbar row (viewer.html:6646-6670) to a non-interactive ribbon on the lanes track (04 F3): now covers EVERY in-lane mark - poll heatmap gradient, selected task/span, waker, parked, kernel sched delay, block_in_place, open-ended poll, CPU sample, sched-blocking, wake, and the local-queue `q:NN` label - not just the legacy subset.
features/02 G17/G18 | amended | T22 | the unified time-aligned column (chunk-2 layout decision) replaces per-lane scroll: the whole track column scrolls as one, so the lanes hold no private vertical-scroll handler (G17) and auto-scroll-into-view (G18) becomes a column-level navigation concern (T23/T33 drive it). The lanes render stacks all worker rows into the one lanes canvas; band geometry scales from the legacy 60px reference.
features/02 M1/M4 | amended | T29 | S6 / issue #282 fix ("global queue invisible at zero", which confused a live Tokioconf audience): the global + max-local + active-task series now render against ONE explicit VISIBLE zero baseline. queue-model.ts `queueScaleY` reserves ZERO_BASELINE_PX (3px) below the lowest data so a 0-valued global renders a flat line above the axis instead of a 1px stroke fused to it; the M4 "0" y-axis label moves to that visible baseline. Numbers are IDENTICAL to the legacy chart (buildQueueRenderModel ports renderQueueChart's bucketing verbatim - same per-bucket max global/local, same maxQ, same active-task maxTasks/startCount): J7 behavioral-diff is same-numbers, presentation-only. Also records the S1/layout adaptation: the collapsed-fold queue panel becomes an always-visible track in the unified column, drawing at draw-area-relative x (canvas is drawW-wide, LABEL_W supplied by the shared DOM gutter - A13) instead of the legacy full-width canvas + inline LABEL_W offset. Legacy viewer keeps the fused-at-zero stroke.
features/02 M5 | amended | T29 | F3(queue): the queue legend moves from the collapsed-fold header swatches (dead with S1) to an always-visible in-track ribbon whose swatch encodings MATCH the drawn series - Global queue (blue area), Max local q:NN (orange line), Active tasks (green line). The in-lane `q:NN` local-queue label keeps its entry in T22's merged lanes legend (features/02 G19); this legend covers the queue TRACK's own encodings.
features/02 M6 | amended | T29 | hover info (Global Q / Local max / Active tasks at the cursor) rerouted from the floating top-right `#info-panel` corner div (I6) to T24's at-cursor store contract (`transient.atCursor` -> the persistent inspector readout, 04 S4): T24's overlay listens for mousemove across the whole track column, so hovering the queue track populates the same readout - no corner div, one at-moment surface. Rendered by T31's inspector (T24's stub until then).
features/02 M7 | amended | T29 | drag-select on the queue track now DISPATCHES the selected time range to `selection.spawnedTasksRange` (new additive selection field); the "tasks spawned in range, grouped by spawn location" derivation is queue-model.ts `computeSpawnedTasks` (a pure port of legacy showSpawnedTasks task-finding + grouping, using taskFirstPoll as the spawn proxy - same numbers, J7). The task LIST + clickable hex task-id links (M8 -> selectedTaskId) RENDER in T31's inspector: T29 dispatches, T31 renders (M7 list / M8 links deferred-until-T31).
features/02 B15 | amended | T34 | New File confirms before discarding a loaded trace (04 S3 "New File discards without confirmation"): the toolbar "New File" click runs window.confirm() on the migrated viewer when a trace is resident; declining is a no-op, confirming OPENS the load chooser (it does not itself discard). The load is non-destructive - the T16 worker only replaces the store `trace` slice on SUCCESS - so a cancelled or failed replace keeps the old trace resident (fixes S3's "no recovery in either direction"). Legacy `btn-reset` (viewer.html:1545-1551) reset to the drop zone with no confirm and no way back. Confirm is injectable (load-controller.ts); Vitest src/pages/viewer/load-controller.test.ts pins the gate, the no-op-on-open, and the keep-old-trace recovery.
features/02 B15/#281 | added | T34 | dismissible load section (GitHub #281: "the load-new-file section cannot be closed once opened; wants Esc + a close button"). On the migrated viewer a chooser opened over a loaded trace is dismissible via Esc (esc-cascade `load` band, priority 90 - below help, above popups) AND a visible close control (the top-right x); both return to the resident trace, and reopening is another New File. The boot chooser (no trace behind) is intentionally NOT dismissible (nothing to return to). No legacy equivalent existed. Vitest pins Esc-close, close-control dismiss, reopen, and the not-dismissible boot case.
features/02 B1/B7/B8 | amended | T34 | presentation reframe of the load surfaces on the migrated viewer, behavior preserved: the drop zone / loading view is a fixed full-cover MODAL layer (sibling of #app, so the shell's declarative re-render never clobbers it) instead of the legacy `#drop-zone` innerHTML swap; the B1 emoji is dropped (matches the T21 F4 empty-state framing, no functional loss); the B8/B9 progress labels keep their phase words and numeric content EXACTLY but normalize the legacy "·"/"…" glyphs to ASCII (" - " / "...") per the repo style rule - "load-timing line kept exactly" holds for the phases (Fetching/Parsing/percent/MB/k-events) and the live wall-clock elapsed suffix. Boot no longer auto-loads the demo (that was a T21 stand-in) - `?trace=` auto-loads, otherwise the drop zone waits and the demo is a link (restores legacy B1/B6). Streaming progress renders in this loading view; the status-bar duplicate is T35's surface. Also lifts the toast region z-index above the load modal so a B13 load-error toast stays visible when a failed load drops back to the drop zone.
features/02 C2-C8 | amended | T33 | the POI toolbar cluster (filter dropdown C2, worst-first checkbox C3, Prev/Next C4/C5, "N/Total" counter C6, jump-to-POI C7, initial setup C8) is replaced by the concept-2 issues rail: a ranked, sortable, keyboard-navigable list (04 S5). Detector source UNCHANGED - the frozen `filterPointsOfInterest` set consumed via lib/trace/analysis (T09); rail count parity holds (demo trace, default filter "sched", worst-first == the legacy "x/74"; pinned by src/pages/viewer/poi.test.ts). C2 filter dropdown kept in the rail header; C3 "worst first" subsumed by column-header sort (worker/kind/time/duration, the duration-desc default == worst-first); C6 counter -> the rail "N/total" position; C7 jumpToPoi centering + task-select math ported verbatim (poi.ts poiJump); `n`/`p` step it (T20 router). Legacy viewer keeps the stepper.
features/02 D1/D2/D3 | amended | T33 | the Blocking Calls / CPU Flamegraph / Heap analysis buttons land in the migrated toolbar with F2 tooltips, conditional visibility (sched delays / hasCpuProfileSamples / alloc events) and count labels. Their region-analysis SURFACES are the inspector/flamegraph tickets (T31/T32): the buttons call an injected seam (a toast until those land), NOT `selection.sidebarRange` directly - setting that range with no sidebar to clear it would soft-lock keyboard selection (H9).
features/02 D6/D7 | amended | T33 | the "Parse perf" button + popup are DEMOTED into the toolbar info menu (the ⓘ <details> disclosure) per F2 (jargon reduction); the menu shows the load/trace summary available from the store (events/workers/duration/truncated) + the uninstrumented-task summary (D4), and notes detailed fetch/parse timing is recorded by the load pipeline (T16/T34).
features/02 E1/E2 | amended | T33 | the Time-mode (Relative/Absolute) and TZ (UTC/Local) toggle BUTTONS land in the migrated toolbar, driving uiPrefs.timeMode/tz (the seam T25's axis reads); TZ button shown only in absolute mode (E2). Legacy globals useAbsoluteTime/useLocalTz retired on the migrated page.
features/02 E3/E4 | amended | T33 | Set Range / Clear Range buttons land with F2 tooltips (Clear Range shown only when the trace is range-filtered); the buffer re-parse they trigger (reparseWithRange) is the load-chrome seam (T34 retains the buffer + URL start/end), so the buttons dispatch an injected callback. The S2 amendment lands here as the goto-time input (`g`, T20) in the time cluster: parse absolute/relative seconds and center the viewport, so "locate the moment" has a mechanism.
store poi slice | added | T33 | new StoreState slice { filter, sortKey, sortDir, index } backing the issues rail; collapses the legacy pointsOfInterest/currentPoiIndex globals into reactive state. The filtered+sorted list is DERIVED (poi.ts), never stored.
features/02 toolbar red-flags chip | added | T33 | concept-2 toolbar summary chip rendering the EXISTING detectors' non-zero COUNTS (scope fence: no new detector source is built - the same filterPointsOfInterest set the rail lists).
features/02 R6 | retired | T31 | legacy Blocking-Calls summary rows carried `cursor:pointer` + `data-leaf` but NO click handler (viewer.html:6119) - a dead affordance (recorded DEAD in the inventory). The G20 pattern: the migrated inspector does NOT port the phantom handler; summary rows render as non-interactive bars. Row listed as retired in the R-row walk, not gated. The live, working drill-in is R7's expandable group stacks, which the inspector keeps.
features/02 P8 | amended | T31 | width persistence was declared-but-DEAD in legacy (the "persist width in storage" intent had no localStorage write, viewer.html:6708-6718; each panel type re-applied its default on fresh open). The migrated persistent inspector IMPLEMENTS it: the resize drag writes `uiPrefs.sidebarWidth` (the store field T06 already declared) AND persists it under `dial9.viewer.sidebarWidth` (the legacy `dial9.viewer.*` key convention), re-read on mount, so a hand-set width survives reload. Trivially in scope of the "resizable" work item; P8 flips DEAD -> VERIFIED.
features/02 I6/S4 | amended | T31 | the at-moment stats readout (Global Q / Local max / Active tasks at cursor), legacy floating top-right `#info-panel`, now renders in ONE place: the persistent inspector's at-cursor readout, from T24's `transient.atCursor` store contract (04 S4 "at-moment stats in ONE surface"). Replaces T24's inspector stub (components/overlay/readout.ts `renderAtCursorStub`, whose CALL is removed from the overlay); the pure compute (`computeAtCursorReadout`) still populates the slice. Surfaces the T17 coverage state (partial/oversized window) rather than presenting a truncated window as whole.
features/02 P1 | amended | T31 | the sidebar is no longer show-on-demand: concept-1's layout makes it a PERSISTENT inspector (chunk-2 layout decision; drift history #291/#304/#305). Selection re-scopes its content in the same action (04 S4) instead of opening/closing the whole surface; the F7 "what is selected" line + explicit clear affordance replace the legacy `x`-close/hide. Esc clears the active content selection (poll/event/range/task) through the T21 esc-cascade (priority `sidebar`) rather than hiding the panel.
features/02 X1-X7 | added | T35 | overview minimap (new surface, 04 S8 "no position context"): a persistent whole-trace overview band with TIER-1 density (aggregate density on coverage `full`, else listing-metadata segment extents+sizes, else the whole-trace event histogram - falls back off a `partial`/`none` aggregate, never presents it as complete), per-bin coverage that distinguishes fetched (`parsed`) from tier-1-only (`listed`/`fetching`/`evicted`/`oversized`) regions with a `partial` badge (T17-audit notes 6-7: an unfetched tail is never rendered as empty/complete), amber POI ticks from the frozen detectors (`filterPointsOfInterest`, the SAME source as T33's rail, no code dep), and a draggable/clickable viewport box that dispatches store viewport updates and keeps the tier-1-only tail navigable. src/pages/viewer/minimap*.ts; Vitest minimap-model/minimap-poi/minimap.
features/02 X8-X14 | added | T35 | status bar (new surface): selection line + explicit clear affordance (04 F7 selection visibility), view range+duration readout, segment fetch/parse progress from the segments slice (the 2.8 feedback hard edge; spinner while fetching), the T19 copy-link button (04 S3 status-line + copy-link SURFACE clause; S3's URL-state clause stays T19's, wired via a `beforeCopyLink` flush seam), and persistent key hints. Fills the shell's `.d9-status` footer host; src/pages/viewer/status-bar.ts; Vitest status-bar (store bindings).
features/02 A shell (minimap+status slots) | amended | T35 | the T21 shell's `.d9-minimap` and `.d9-status` placeholder slots become empty ARIA hosts that mountMinimap / createStatusBar fill imperatively (the toast/legend technique - the shell's declarative re-render never orphans the components' children); the shell view model drops its interim `selectionLabel`/`viewRangeLabel` (now owned by the status bar). Localized to the two slot regions + their create/dispose wiring (shell.ts, main.ts).
features/02 O1/O2/O3 | amended | T36 | per-panel one-line FOLD -> per-track COLLAPSE under the unified column. A caret button in the manageable analysis tracks' (cpu/queue/spans/events - section-O scope) label gutter toggles the track to label-only height (COLLAPSED_TRACK_H=24, matching legacy 24px); CSS `.d9-track-manage.is-collapsed` hides the drawing body. The caret is a real `<button aria-expanded>` (O2 keyboard collapse preserved: Tab + Enter/Space) with a CSS-driven chevron (O3: down expanded / right collapsed). The legacy "click anywhere on a collapsed body" affordance is dropped for the single explicit caret; the overlay strip is pointer-events:none except the caret+grip so the spans copy buttons stay interactive. timeline/lanes/task-detail are pinned (task-detail was never foldable). Legacy viewer keeps the folds. Vitest src/pages/viewer/track-management.test.ts.
features/02 O4 | amended | T36 | localStorage persistence reshaped: the legacy per-panel `dial9.viewer.panelCollapsed.<key>` (collapsed/expanded) string keys are replaced by ONE JSON blob under `dial9.viewer.trackPrefs` = { trackOrder, collapsed } (O-collapse + O8-reorder share it). hydrateTrackPrefs seeds uiPrefs on boot before first paint; mountTrackPrefsPersistence writes on change; the try/catch in-memory fallback is preserved (mirrors viewer.html:1410-1426). Default is EXPANDED not all-collapsed (the S1 amendment). Headline DoD - survives reload - pinned by the save->fresh-store->hydrate round-trip test. Legacy per-panel keys are NOT read on the migrated page (raw switch, no state porting - T38).
features/02 O5/O6/O7 | amended | T36 | legend sync + render-on-toggle + expanded-only labels fold into the unified column mechanics: O5/O7 legend hiding is subsumed by the whole-body hide (span/event/queue legend strips now live inside each track body - T26/T27/T29 - so `.is-collapsed .d9-track-body` hides them with it); O6 renderAll-on-toggle becomes a coalesced uiPrefs dispatch (RAF scheduler, <=1 render/frame - F2), and sizeTracks skips a collapsed track then re-paints from CURRENT windowed state on re-expand (carried T17-audit notes 6-7: windowing respected).
features/02 O8 | added | T36 | NEW track drag-reorder: a grip in each manageable track's label gutter reorders whole tracks; drop onto another manageable track SWAPS positions (no animation, per the concept-1 interaction spec). timeline/lanes/task-detail stay pinned. The resolved manageable order persists in uiPrefs.trackOrder (same dial9.viewer.trackPrefs blob) and survives reload; orderedTracks is robust to stale/partial stored orders (unknown/duplicate/non-manageable ids dropped; a newly-added track appears in its catalogue slot). Track PINNING is explicitly OUT of scope (no section-O contract; optional future ledger addition). No legacy equivalent.
store uiPrefs trackOrder/collapsed | added | T36 | two additive UiPrefsSlice fields (trackOrder: string[], collapsed: Record<string,boolean>) declared in types/state.d.ts backing O-collapse + O8-reorder; seeded empty in the viewer store (S1 resting default = catalogue order, nothing collapsed). The legacy panelCollapsed field is left as a superseded no-op holder (no live surface reads it; additive scope). Full-slice fixtures (exhaustive.test.ts, store.test.ts) kept complete.
features/02 S F15/F16 | amended | T32 | the region CPU flamegraph (F15) and heap flamegraph (F16) render EMBEDDED in the migrated viewer's persistent inspector Stack tab (concept-1), not a show-on-demand sidebar. Driven by the frozen flamegraph.js widget through its EXISTING public surface (createFlamegraph / setData / getZoomPath, via the lib/canvas seam) - the frozen core is untouched (AGENTS.md frozen-core list). Region-scoped sample building, the S7 count, and the Horvitz-Thompson heap estimates (invP=1/(1-exp(-size/R)), R=524288) + Bytes/Count toggle + hook-frame stripping are the pure region-analysis-model, ported verbatim from showFlamegraph/showHeapFlamegraph; counts/estimates behavioral-diff via src/pages/viewer/region-analysis-model.test.ts (19 cases). P6 auto-widen-on-flamegraph is dropped (the persistent inspector is user-resizable, P8; the widget fits the current width). Legacy viewer keeps the show-on-demand sidebar.
features/02 R3-R9 | amended | T32 | the region blocking-calls / scheduling panel (showSchedPanel) was DEFERRED from T31 to T32 by the merged lane-interaction seam (Shift+drag -> selection.sidebarRange; "what opens is T32's", lane-interaction.ts:14-18). It renders in the migrated inspector Stack tab: group-by blocking-call/full-stack (R4), summary bars with the 5-color leaf palette lock/io/poll/syscall/other (R5), expandable unique full-stack sub-groups with tokio-runtime dimming (R7), and up to 5 example-poll jump links centering the viewport (R8). buildBlockingView ports collectSchedSamples + renderSchedPanel grouping/counts verbatim. R6 stays retired (T31); R9 empty states render as inline hints (persistent inspector) rather than the legacy toasts.
features/02 S7 | amended | T32 | (04 S7 / iss #571) count-contradiction ROOT-CAUSED, not a bug: the toolbar "Flamegraph (8993)" counts trace.cpuSamples.length - ALL sample records (on-CPU + off-CPU/scheduling source===1 + stackless); the flamegraph "147 samples" counts only the FOLDABLE on-CPU-with-stacks subset (the frozen filterCpuSamples predicate callchain.length>0 && source!==1, flamegraph.js:129). Different UNITS - the toolbar advertises data volume (04 "Works well"), the panel shows what actually folds. Reconciled ON-SCREEN: the embedded CPU panel labels its count "N samples (on-CPU, with stacks) of M CPU records" (cpuCountLabel) so the two never read as a contradiction on one page. The T33 toolbar button label (whole-trace cpuSamples.length) is left unchanged (T33-owned surface); no fix needed - it is a correct volume indicator.
features/02 F20 region frame->timeline | added | T32 | (04 S7 "flamegraph severed from time" fix; NEW inventory row F20 added in-diff) zooming a CPU flamegraph frame offers "Show in timeline", navigating the viewport to the [min,max] timestamp extent of that frame's samples within the analyzed region (padded, clamped). frameSampleTimeExtent reproduces buildFlamegraphTree's per-node display-name path EXACTLY (reversed chain + inline-frame expansion + formatFrame) and prefix-matches the frozen getZoomPath() names, so NO core edit is needed. Worker panel wins when both panels are zoomed; CPU-mode only (heap frame->time would need alloc timestamps carried - noted, small follow-on).
features/02 F19 | amended | T32 | Pop Out on the migrated viewer opens the legacy flamegraph.html (base href "/") preserving ?trace= URL(s), start/end, and per-panel worker-zoom/offworker-zoom paths - byte-faithful to legacy F19. The in-memory (dropped-file) blob-URL fallback is NOT ported: a trace with no ?trace= URL surfaces the F19 "requires a URL-loaded trace" error toast (the store retains the parsed trace, not the raw buffer). Pop Out shows on the CPU flamegraph only, as legacy.
features/02 D1/D2/D3 | amended | T32 | (completes the T33 seam) the toolbar Blocking Calls / CPU Flamegraph / Heap buttons now OPEN the T32 region analyses for the whole trace via the onOpenAnalysis callback T33 injected (previously a toast). openWholeTrace sets selection.sidebarRange to [minTs,maxTs] (activating the Stack tab, P4) with a forced mode, so the requested analysis opens even when other data is present. Presentation note: reusing the retained-region mechanism boxes the analyzed scope via the H10 selection overlay (legacy toolbar opens drew no box) - an intentional analyzed-scope indicator, not scope-creep on the drag gesture.
features/02 S partial-window badge | added | T32 | (T17-audit notes 6/7; 2.8 "never silently wrong") the region panel shows a "partial window" / "oversized segment" badge when a segment overlapping the analyzed region is not resident (parsed) - the chunk-2 consumer of T17's truncation/coverage signal the audit required. regionCoverage over the segments slice; complete (no badge) on the non-windowed whole-trace load and when every overlapping segment is parsed.
features/02 F17/F18 | deferred | T32 | idle-time flamegraph (F17; task-detail N4 link) and task-dump stack (F18; lane idle-gap N11 click) are DEFERRED: both are task-scoped (not range-scoped) analyses whose access paths are T30 (task-detail idle link) and T22 (lane idle-gap click) surfaces NOT wired at HEAD, and the selection-driven inspector does not model a task-scoped analysis surface without a T30/T22 dispatch change (outside T32's owned section-S rows). The region-panel rendering seam is built to host them; the flamegraph-sample building is a small follow-on when those dispatches land. The ticket GOAL (Shift+drag -> flamegraph/blocking/heap + S7) is complete. Deferred, not retired.
features/02 K7 | deferred | T32 -> T47 | keyboard flamegraph frame traversal (arrows walk the tree + Enter zoom, 04 K7) DEFERS to a core API addition per the ticket. The frozen flamegraph.js public surface exposes getZoomPath (read) + zoomToPath (append-from-EMPTY-stack, for URL restore) + handleEscape (cascade reset) but NO absolute setZoomPath / resetZoom / zoomToNode, so keyboard traversal cannot drive the widget cleanly without editing the frozen core (forbidden here). T47 line item filed (chunk-3-post.md, core-reshape ADR partition). Escape-to-reset-zoom already works via the widget's handleEscape in the inspector esc-cascade.
features/02 C1a | added | T45 | (closes #68, open since early: SegmentMetadata parsed but never shown) trace-EMBEDDED service/host from ParsedTrace.segmentMetadata surfaced as chips in the toolbar file-info area. The `service`/`host` key NAMES are a writer-side convention (NOT a wire contract), confirmed literal against the writer - dial9-utils S3 source `.metadata("service"|"host",...)`, gen_fixtures standard_metadata(), the dial9-core writer roundtrip test, and the metrics-service example - and pinned once in lib/trace/segment-metadata.ts (SEGMENT_SERVICE_KEY/SEGMENT_HOST_KEY) with the shape in types/trace.d.ts (SegmentIdentity), the "one small contract addition". RECONCILED with the S3-key-derived svc/host URL params (W11, the distinct source the browser page appends): embedded metadata WINS when both are present, a disagreeing key-derived value is tooltipped, and key-derived shows as a fallback when the trace carries no embedded metadata (restores the structured-metadata display the T33 C1 port dropped). Demo trace carries `service` (metrics-service) but NO `host`, so only the service chip walks there; both walk via the T42 window fixture (service=svc-fix, host=window). Vitest src/lib/trace/segment-metadata.test.ts.
features/04 census +#d9-ui-switch | added | T41 | Dual-UI switch control (T38). Registering tokio_stats.html in NEW_UI_ENTRIES makes the always-visible control render on BOTH generations, with per-side label/href ("Switch to new UI" -> /new/tokio_stats.html vs "Switch to legacy UI" -> /tokio_stats.html?ui=legacy). The old-vs-new affordance-census diff carries exactly ONE CHANGED entry for #d9-ui-switch; everything else is zero-diff (the seed's default single-period state renders no tabs and no exemplar links - both NOT-TRIGGERABLE / threshold-gated - so those onclick-vs-@click affordances never appear on either side; the `?` help overlay is kbd/span/h2 content, not a census-selected affordance).
features/04 K1 | amended | T41 | unified `?` help overlay on the migrated page (T20 K3). K1 recorded "no keyboard model" on the legacy page; the migration adds the ONE unified binding (`?` toggles a help overlay listing the page's controls, Esc closes it) - a previously-dead key, so "existing bindings unchanged" holds vacuously. Keys dispatch/toggle chrome only, never render (architecture 2.5). Legacy page keeps zero keyboard.
features/04 H4 | preserved | T41 | fix-vs-preserve DECISION recorded for maintainer sign-off (finding 1): the exemplar deep link still targets /api/trace (removed by #582, so it 404s), carried BYTE-IDENTICALLY on the migrated page rather than repointed to /api/object. Rationale: T41 is a behavior-preserving port (same treatment as T13/T14 - defects carried, not fixed), and the DoD requires the census/behavioral differ to show ONLY the switch delta; repointing would introduce a second, non-switch delta. The one-line fix (exemplarLink -> /api/object?bucket&key) is deferred to a follow-up if the maintainer wants it live.
features/01 F6-contrast | amended | T37 | 04 F6 stragglers on the migrated browser page (styles/browser.css, ported VERBATIM by T14 with restyle deferred): three muted greys failed axe color-contrast on this all-dark page - footer #444 on #1a1a2e (~1.75:1, worst node; also F4's low-contrast footer that hosts the demo/drop affordances), and header meta + empty-state/loading status #666 on #16213e (~2.8:1). Lifted to muted greys that clear the 4.5:1 AA bar (footer #8b8ba3, header span + .status #9a9ab2). Brighter text on an all-dark page can only raise contrast, never regress it. Legacy page keeps the low-contrast palette. Live axe re-confirmation across the four pages is a T37 trailing item; the borderline #888-on-#16213e labels (~4.48:1) are flagged there.
features/02 K4 f-fit | added | T37 | viewer `f` fit key (04 K4 fit-key sub-gap): the migrated viewer had `z` zoom-undo (T23) but no fit key, though the `?` help (help.ts:23-25) advertised `f`. Bound `f` in lane-interaction.ts to the EXISTING viewport.fit() (viewport-actions.ts:158, doc-commented "H3/`f`", already driven by the "Fit all" mouse button and already committing to zoom history so `z` undoes it). No new mechanism; makes the advertised key live and matches the flamegraph's `f` for one vocabulary (04 K3). Declines (falls through) when no trace is loaded; announces via A16. Legacy viewer keeps the mouse-only Fit button.
features/02 rail width | added | #795 | the issues/tasks rail is drag-resizable at its right edge (min 220px, max 60vw; handle mirrors the inspector's), so long spawn locations in the Tasks tab read without hovering. Width lives in uiPrefs.railWidth (default 300 = the previous fixed width), persists in the dial9.viewer.trackPrefs blob, and is URL state via the NEW additive `rail-width` query param (positive CSS px). Vitest: issues-rail (clamp), track-management (persist round-trip), url-state (param round-trip).
features/02 task column widths | added | #795 | per-column resize for BOTH rail tables - Tasks and Issues (follow-on to the rail width, same review): each sortable header carries a right-edge divider (the Issues severity-dot column is seeded/pinned but not draggable, stored under the `dot` key); the FIRST drag seeds all six columns from their rendered widths and the table switches to fixed layout (colgroup + table width = sum, horizontal scroll inside the rail when it exceeds the rail width), so one column changes while the rest hold. Double-click any divider resets to automatic content-fit layout. Widths live in uiPrefs.taskColWidths / uiPrefs.issueColWidths (shrink floors at the header label's measured width so it never clips; resized headers center in their column), persist in the dial9.viewer.trackPrefs blob, and are URL state via the NEW additive `task-cols` / `issue-cols` query params (v1: + TAB-joined key,px entries; unknown keys and non-positive widths dropped on read). Vitest: issues-rail (clamp), track-management (round-trip + tolerance), url-state (param round-trip).
features/02 D1/D2/D3 whole-trace box | amended | #796 | the T32 "analyzed-scope indicator" ruling is reversed for the whole-trace case: the toolbar Blocking Calls / CPU Flamegraph / Heap opens retain selection.sidebarRange=[minTs,maxTs] as before (Stack tab activation, Esc-cascade, URL region param unchanged), but the H10 selection overlay now draws NO retained box when the range covers the whole trace extent - users read the full-page blue tint as a rendering bug, and a whole-trace analysis has no sub-range to distinguish (the Stack tab already names its scope). Partial regions keep the box. Vitest: selection-overlay (whole-extent -> null, extent-minus-1ns -> box).
